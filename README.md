# apov-js-navigator
```
Interactive streaming of pre-rendered voxels region written in javascript.

Please see atomic-point-of-view project to generate the atoms-done.bin file.

Then simply use the following to run the project:
npm install \
    cd client; npm install \
    cd ..; npm run both
